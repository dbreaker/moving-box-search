<%= form_for(@move_size) do |f| %>
  <% if @move_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@move_size.errors.count, "error") %> prohibited this move_size from being saved:</h2>

      <ul>
      <% @move_size.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :home_size %><br />
    <%= f.text_field :home_size %>
  </div>
  <div class="field">
    <%= f.label :pounds %><br />
    <%= f.text_field :pounds %>
  </div>
  <div class="field">
	  <b>UsedCardboardBox Products:</b>
		<ul>
		<% @used_card_board_box_products.each do |u|%>
		<li><%= check_box_tag("used_bard_board_box_product[#{u.id}]", 1) %>  <%= u.name %></li>
		<% end %>
		</ul>
  </div>
	
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
